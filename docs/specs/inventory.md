---
schema: specification
title: 在庫仕様
domain: INV
---

# 在庫仕様

## 結論と対象範囲

在庫は商品の販売可否を判断する基準である。注文確定時に最新の在庫を確認し、注文の保存と在庫の減算を一体として扱う。

## 管理データ

在庫は、商品、販売可能数、更新日時を持つ。更新できるのは注文確定処理、注文の取り消し処理、Admin以上のロールを持つ担当者による手動修正だけである。保持期間は[`Q-OPS-003`](../meta/questions/Q-OPS-003-data-retention.md)で決定する。

販売可能数は0以上の整数とし、負数は保持しない。

## SPEC-INV-001 在庫に基づく販売可否を表示する

関連要件: `REQ-INV-001`、`REQ-VIS-002`

来場者向けメニューと注文・会計画面は、現在の在庫に基づく販売可否を表示する。在庫がない商品は販売可能として表示しない。

### 受け入れ例

1. 在庫がある商品とない商品を用意する。
2. メニューと注文・会計画面を開く。
3. 両画面で販売可否が在庫と一致する。

## SPEC-INV-002 注文確定直前に在庫を再確認する

関連要件: `REQ-INV-002`、`REQ-SAL-005`

注文候補の表示時点ではなく、注文確定時点の在庫を最終判定に使う。いずれかの商品が不足する場合は、不足商品と確定できない理由を表示し、注文と在庫を変更しない。

不足が判明した後、会計担当者は注文候補の個数変更と商品の削除で対応する。代替商品の提示はシステムでは行わず、来場者と口頭で相談して入力し直す。

### 受け入れ例

1. 注文候補を作成した後、別の注文によって対象商品の在庫を不足させる。
2. 元の注文候補を確定する。
3. 不足商品が表示され、注文件数と在庫が変化しない。

## SPEC-INV-003 注文保存と在庫減算を一体で行う

関連要件: `REQ-INV-003`、`REQ-SAL-005`、`REQ-SYS-002`

注文確定に成功した場合は、確定した数量だけ在庫を減らす。処理途中で失敗した場合は、注文だけが保存された状態や、注文がないまま在庫だけが減った状態を残さない。

複数の会計操作が同じ在庫を同時に確定しようとした場合も、在庫数を超える注文を成立させない。

### 受け入れ例

1. 注文確定処理の各段階で失敗を発生させる。
2. 処理結果を確認する。
3. 注文と在庫がともに更新されるか、ともに更新されていない状態になる。
4. 同じ在庫数を超える複数注文を同時に確定しても、確定数量の合計が在庫数を超えない。

## SPEC-INV-004 在庫を登録・修正する

関連要件: `REQ-INV-001`

Admin以上のロールを持つ担当者は、営業開始前に商品ごとの初期在庫を登録する。営業中に実在庫とずれた場合は、現在の数量を直接入力して上書きする。増減の差分ではなく、数え直した結果をそのまま入力する。

修正では、修正前後の数量、修正した担当者、日時を記録する。

### 受け入れ例

1. Admin以上の担当者が商品の初期在庫を登録する。
2. 注文確定によって在庫が減った後、実在庫を数え直して現在の数量を入力する。
3. 在庫が入力した数量になり、修正前後の数量と担当者が記録される。
4. Staffとして同じ修正操作を試みると成立しない。
