# AGENTS.md

## Chat

- 日本語でフレンドリーに、本題から簡潔に答える。空虚な称賛や無条件の肯定を避け、ユーザーの意向を尊重しつつ根拠に基づいて意見する。判断は新しい事実や証拠がある場合に変える。文体は丁寧体(です・ます調 / 敬体)で返答すること。
- 自然で読みやすい日本語を使い、一般的な訳語やカタカナ表記を優先する。コード識別子・コマンド・パス・正式名称などは原表記を保ち、コード上の名前やコマンドはバッククォートで囲む。原語の併記は理解や参照に必要な初出に限る。
- ドキュメント・PR の説明文・コメントは日本語、PR のタイトル・コミットメッセージは英語で記述する。個別の指定がある場合はそれに従う。
- 技術的な主張は鵜呑みにせず、プロジェクトの判断ではローカルのコード・文書・設定・テストを先に確認する。外部仕様など変わりうる情報は Web で最新情報を確認し、一次情報を優先する。Web を参照した回答には情報源の名称と URL を示す。
- ライブラリ・フレームワーク・SDK・API・CLI・クラウドサービスの使い方に関する質問では Context7 を優先する。正確なライブラリ ID がなければ `resolve-library-id` で特定し、`query-docs` で論点ごとに確認する。一般的なプログラミング概念やコードレビューなどには使わない。
- 事実と推論・提案を区別し、情報が古い・矛盾する・未確認の場合は確信度と不明点を明示する。重要な判断では前提・リスク・代替案と、それぞれのメリット・デメリットを説明する。
- 曖昧な依頼でも安全に進められる場合は仮定を明示して進める。要件が大きく分かれる場合や、破壊的・取り返しにくい操作では事前に確認する。長時間・高コスト・外部への書き込みを伴うコマンドは目的と影響範囲を示し、明示的な承認を得る。
- 完了時は変更点・実施した検証・未検証事項と理由・残るリスクを簡潔に報告する。目標を達成できない場合は阻害要因と代替案を示す。

## Documentation

- 日本語のドキュメントの文体は普通体(だ・である調 / 常体)で記述する。
- ドキュメントを追加・更新・削除する前に、`docs/documentation-management.md` を読み、正本、ID、テンプレート、更新フロー、確認項目に従う。
- ドキュメントには現在の正しい状態だけを書く。過去に検討した案、中止・削除した機能、以前の挙動、編集経緯は残さない。
- 「別のリリースで実装する」「方針変更により削除した」など、現在の判断や行動に不要な注記を本文へ加えない。履歴が必要な場合は Git のコミット、プルリクエスト、CHANGELOG、ADR のうち目的に合う場所へ記録する。
- プロジェクト概要と要件文書は、初めて読む人が最短で現在の目的、対象範囲、業務フロー、必要な判断を把握できる構成にする。人間の読みやすさを最優先にし、Agent向けの構造化は検索と参照に必要な範囲に限定する。
- 仕様は「画面」「データ」「機能」「品質」「受け入れ」のような横割りで分けず、`docs/specs/` に業務能力・ドメインごとにまとめる。
- 現在の仕様を説明する文書では、読者の判断、行動、安全性、認識の一致に寄与しない背景説明、重複、言い換え、将来の可能性を削る。
- ある程度の要件と実現可能性を説明できる機能計画だけを、`docs/meta/plans/` に機能ごとのファイルとして置く。単なる思いつきは要件、仕様、機能計画へ加えない。
- 計画を正式な要件へ移す場合は、関連する要件TOML、仕様、提供範囲を現在の正しい状態へ更新し、元の計画文書を削除する。

## Code Comments

- コメントは原則書かない。意図は名前、型、関数の分割、テストの題名で伝える。
- コメントを書く前に次を順に試し、伝わるならコメントを書かない。
  1. 名前を変える。`price` に「円単位」と注釈せず `priceYen` にする。
  2. 型で表す。値の制約は `Schema` の制約として書く。
  3. テストの題名で表す。`it("在庫の記録がない商品を販売可能としない")` のように、規則そのものを題名にする。
- 書いてよいコメントは次の3つだけである。
  - 外部システムやライブラリの制約で、コードだけでは不自然に見える箇所。例: D1に対話型のトランザクションがない、Elysiaの事前コンパイルはWorkerの起動時にしか行えない。SQLや言語の一般的な知識は制約に含めない。
  - 自然に見える別の書き方へ変えると壊れる箇所。何をすると何が壊れるかを書く。例: `leftJoin` の右側が null の行を捨てると、原材料が未確認の商品がメニューから消える。
  - テストで表現できない不変条件。
- 判断の理由は `docs/meta/decisions/` の DEC に一度だけ書く。コードへ同じ理由を書かず、参照が必要な場合はファイルにつき1回、`DEC-SYS-005` のようにIDだけを書く。同じ理由を複数のファイルへ書かない。
- ファイルやシンボルを「なぜここに置くか」「どの層に属するか」で説明するコメントを書かない。配置と依存の向きは DEC とリンターの設定が正本である。
- `src/` 配下の本番コードに `REQ-*`、`SPEC-*`、`docs/` 配下のパスを書かない。仕様との対応は、テストの `describe` の題名に仕様IDを含めて持つ。例: `describe("SPEC-INV-001 在庫に基づく販売可否")`。
- 識別子、型、直後のコードを日本語に言い換えただけのコメントを書かない。
- テストの本文にコメントを書くのは、題名だけでは誤読する罠がある場合に限る。
- 編集対象のファイルに上の条件を満たさないコメントがあれば、同じ変更の中で削除する。削除する前に、その理由が DEC に書かれているかを確認し、書かれていなければ DEC へ移す。

## Pull Request

- ブランチを切る前とプルリクエストを作る前に、`docs/change-management.md` を読み、変更の単位、手順、承認方法に従う。

## Local Project Prompt

リポジトリのルートディレクトリに `AGENTS.local.md` ファイルが存在する場合、作業を開始する前にその内容を読み、このファイルをプロジェクトの追加指示として扱う。

指示内容が矛盾する場合、より優先度の高いシステム指示、開発者指示、またはユーザー指示が存在しない限り、`AGENTS.local.md` の内容を優先して適用する。

明示的に指示されていない限り、`AGENTS.local.md` の内容を変更したりコミットしたりしない。
